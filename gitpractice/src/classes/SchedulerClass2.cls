global class SchedulerClass2 implements Schedulable{
global void execute(SchedulableContext sc){  
 //Batchexample atr=new Batchexample();
//id batchinstanceid = database.executeBatch(atr); 
    system.debug('batchscheduling');
//ID jobID = System.enqueueJob(new Queueablexample2());
//Futureexample.testusecases();
	}
}